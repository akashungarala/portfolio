import { describe, expect, it } from 'vitest';

// TDD Step 1: Write failing test for a utility function that doesn't exist yet
describe('utils', () => {
  describe('cn (className merge utility)', () => {
    it('should merge class names correctly', () => {
      // This test will fail until we implement the cn function
      // For now, just test the test framework works
      expect(true).toBe(true);
    });
  });
});
